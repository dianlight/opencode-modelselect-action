group:
  repos: |
    dianlight/smartmontools-sdk
    dianlight/hasos_more_modules
    dianlight/srat
    dianlight/hassio-addons
  files: []
  # No files are synced. Model selection needs no sync — downstream
  # workflows call the select-model action, which reads the live config here.
  # NOTE: opencode-pr-review.yml, opencode-pr-comment.yml,
  # opencode-issue-handler.yml and .github/scripts/auth.sh were deprecated
  # and deleted; removal in target repos is handled by the sync-actions
  # cleanup PR job.
