{
  "name": "opencode-modelselect-action",
  "version": "1.0.0",
  "private": true,
  "description": "Opencode Modelselect — preselect the OpenCode model for a task-type and tier from the central model config",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/",
    "coverage": "node --test --experimental-test-coverage test/"
  }
}
